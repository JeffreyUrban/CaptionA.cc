/**
 * Schema Loader - Client-side stub
 * Server-side file system functionality removed for SPA mode
 */

/**
 * Get schema file path (stub - not functional in browser)
 */
export function getSchemaPath(version: number): string {
  throw new Error('getSchemaPath is not available in SPA mode - server-side only')
}
