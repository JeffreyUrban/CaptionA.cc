# Fly.io configuration for CaptionA.cc API (Production)
# Deploy: fly deploy -c fly.prod.toml

app = "captionacc-api-prod"
primary_region = "ewr"

[build]
  dockerfile = "Dockerfile"
  build-target = ""
  ignorefile = ".dockerignore"
  [build.settings]
    builder = "depot"
  [build.args]
    PREFECT_FILE = "prefect.yaml"

[deploy]
  # Run before each deployment to register Prefect flows
  release_command = "bash scripts/deploy_flows.sh"

[env]
  ENVIRONMENT = "production"
  API_HOST = "0.0.0.0"
  API_PORT = "8000"
  CRSQLITE_EXTENSION_PATH = "extensions/linux-x86_64/crsqlite.so"
  WORKING_COPY_DIR = "/data/working"
  WASABI_UPLOAD_IDLE_MINUTES = "5"
  WASABI_UPLOAD_CHECKPOINT_MINUTES = "15"
  LOCK_EXPIRY_MINUTES = "30"
  SUPABASE_SCHEMA = "captionacc"
  MODAL_APP_SUFFIX = "prod"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

kill_timeout = "30s"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "captionacc_data_prod"
  destination = "/data"
  initial_size = "1gb"

[checks]
  [checks.health]
    grace_period = "60s"
    interval = "30s"
    method = "get"
    path = "/health"
    port = 8000
    timeout = "10s"
    type = "http"
