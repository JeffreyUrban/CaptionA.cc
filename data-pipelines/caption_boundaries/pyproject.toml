[project]
name = "caption-boundaries"
version = "0.1.0"
description = "Caption boundaries processing"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
